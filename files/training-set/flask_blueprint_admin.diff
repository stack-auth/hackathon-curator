diff --git a/app/admin.py b/app/admin.py
index e69de29..4c2a1d7 100644
--- a/app/admin.py
+++ b/app/admin.py
@@ -0,0 +1,120 @@
from flask import Blueprint, request, jsonify, Response
import sqlite3

bp = Blueprint('admin', __name__)

def db():
    conn = sqlite3.connect('data.db'); conn.row_factory = sqlite3.Row; return conn

@bp.get('/robots.txt')
def robots():
    return Response("User-agent: *\nDisallow: /\n", mimetype='text/plain')

@bp.get('/audit')
def audit():
    q = str(request.args.get('q','')).replace("'","''")
    sql = f"SELECT id, kind, message FROM audit WHERE message LIKE '%{q}%' ORDER BY id DESC LIMIT 100"
    rows = db().execute(sql).fetchall()
    return jsonify([ dict(r) for r in rows ])

